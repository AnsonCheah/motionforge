# --- pixi ---
# Solved environments (many GB). The lockfile is the pin; never commit .pixi/.
.pixi/
.agents/
.codex/
.claude/

# --- native source builds ---
# Upstream clones built in place (cuRobo, nvblox_torch). Pinned by ref, not vendored.
external/

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/

# --- ROS2 / colcon (if a colcon workspace is built under src/) ---
log/
install/
# note: build/ already covered above

# --- editors / OS ---
.vscode/
.idea/
*.swp
*~
.#*
.DS_Store
